import { expect } from "chai";

describe("true", () => {
    before(async () => {
        // Do some async setup.
    });
    it("should be true", () => {
        expect(true).to.equal(true);
    });
});